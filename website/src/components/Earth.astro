---
---
<div>
    <style>
        img#planet {
            position: fixed;
            width: 800px;
            height: 800px;
            left: -320px;
            bottom: -320px;
            animation: rotate 50s linear infinite;
        }

        @keyframes rotate {
            0% {
                transform: rotate(0deg);
                /* filter: hue-rotate(0deg); */
            }
            100% {
                transform: rotate(360deg);
                /* filter: hue-rotate(360deg); */
            }
        }
    </style>
    <img src="/redplanet.png" id="planet" />
</div>