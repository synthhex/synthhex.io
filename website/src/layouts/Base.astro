<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="Synthhex.io - A personal website." />
    <meta name="author" content="Synthhex" />
    <meta name="theme-color" content="#0f1a0b" />

    <title>Synthhex.io</title>

    <link rel="icon" type="image/svg+xml" href="/synthhex.svg" />
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    
    <style is:global>
        * {
			box-sizing: border-box;
            scroll-behavior: smooth;
		}

		html, body {
			width: 100%;
			height: 100dvh;
            margin: 0;
            padding: 0;
            overflow: hidden;
            overscroll-behavior: none;
		}

        @font-face {
            font-family: 'Exo 2';
            src: url('/fonts/Exo2-Variable.woff2') format('woff2');
            font-weight: 700;
            font-style: bold;
            font-display: swap;
        }

        @font-face {
            font-family: 'Nunito Sans';
            src: url('/fonts/NunitoSans-Variable.woff2') format('woff2');
            font-weight: 400;
            font-stretch: 100%;
            font-style: normal;
            font-display: swap;
        }

		body {
            color: white;
            font-family: 'Nunito Sans', sans-serif;
            overflow-wrap: break-word;
            background-color: #080e17;
			background-image: url('/stars.png'), linear-gradient(45deg,#0f1a0b,#080e17, #080817);
        }

        h1, h2, h3 {
            font-family: 'Exo 2', sans-serif;
            vertical-align: middle;
        }

        h1 {
            font-weight: 700;
            vertical-align: middle;
        }

        p {
            font-weight: 500;
            font-size: 1.1em;
            vertical-align: middle;
        }

        a:link {
            color: white;
        }

		.container {
			padding: 5px;
			width: 100%;
			height: 100%;
		}

		.column {
			display: flex;
			flex-direction: column;
			align-items: center;
		}

        .row {
            display: flex;
            flex-direction: row;
            align-items: center;
        }
	</style>
</head>
<body>
	<div class="container column" id="signs">
        <style>
            #signs {
                overflow: hidden;
                overflow-y: scroll;
                position: relative;
            }
        </style>
		<slot />
	</div>
</body>
</html>
